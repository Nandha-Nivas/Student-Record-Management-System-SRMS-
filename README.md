# Student-Record-Management-System-SRMS-
A C - based Student Record Management System with role-based login (Admin/Staff/Guest). Supports adding, viewing, searching, updating, and deleting student records using text-file storage. Includes input validation, case-insensitive search, and safe file rewriting via a temporary file.
